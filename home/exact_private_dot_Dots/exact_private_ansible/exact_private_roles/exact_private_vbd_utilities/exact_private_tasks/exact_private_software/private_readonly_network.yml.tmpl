# chezmoi:template: left-delimiter={@' right-delimiter='@}
{@'- if false -'@}
# vim: set ft=yaml:
{@'- end -'@}
{@'- if or .sys.isDebianBased .sys.isVoid -'@}
{@' includeTemplate .tmpl.msg.managedByChezmoi . | comment "# " '@}
---

{@'- if .sys.isDebianBased '@}
{@'- /* {{{ Debian-based */'@}

- name: Network utils | Install packages (stable)
  tags: [packages, system, utilities, network_utils]
  become: true
  ansible.builtin.apt:
    package: "{{ vbd_utilities_pkgs_network.stable }}"
    state: latest
    install_recommends: false

- name: Network utils | Install packages (backports)
  tags: [packages, system, utilities, network_utils]
  become: true
  ansible.builtin.apt:
    package: "{{ vbd_utilities_pkgs_network.backports }}"
    default_release: "{{ ansible_facts['distribution_release'] }}-backports"
    state: latest
    install_recommends: false

{@'- /* }}} */'@}
{@'- else if .sys.isVoid '@}
{@'- /* {{{ Void */'@}

- name: Network utils | Install packages (xbps)
  tags: [packages, system, utilities, network_utils]
  become: true
  community.general.xbps:
    package: "{{ vbd_utilities_pkgs_network.xbps }}"
    state: present

{@'- /* }}} */'@}
{@'- end '@}

...
{@'- end '@}
