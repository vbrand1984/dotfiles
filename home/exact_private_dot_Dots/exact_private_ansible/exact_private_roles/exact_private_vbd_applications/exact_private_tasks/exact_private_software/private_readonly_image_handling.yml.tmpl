# chezmoi:template: left-delimiter={@' right-delimiter='@}
{@'- if false -'@}
# vim: set ft=yaml:
{@'- end -'@}
{@'- if or .sys.isDebianBased .sys.isVoid -'@}
{@' includeTemplate .tmpl.msg.managedByChezmoi . | comment "# " '@}
---

{@'- if .sys.isDebianBased '@}
{@'- /* {{{ Debian-based */'@}

- name: Image handling apps | Install packages (stable)
  tags: [packages, desktop, applications, image_handling]
  become: true
  ansible.builtin.apt:
    package: "{{ vbd_applications_pkgs_image_handling.stable }}"
    state: latest
    install_recommends: false

{@'- /* }}} */'@}
{@'- else if .sys.isVoid '@}
{@'- /* {{{ Void */'@}

- name: Image handling apps | Install packages (xbps)
  tags: [packages, desktop, applications, image_handling]
  become: true
  community.general.xbps:
    package: "{{ vbd_applications_pkgs_image_handling.xbps }}"
    state: present

{@'- /* }}} */'@}
{@'- end '@}

...
{@'- end '@}
