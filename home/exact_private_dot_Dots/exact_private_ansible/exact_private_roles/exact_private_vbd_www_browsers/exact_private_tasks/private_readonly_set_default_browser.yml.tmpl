# chezmoi:template: left-delimiter={@' right-delimiter='@}
{@'- if false -'@}
# vim: set ft=yaml:
{@'- end -'@}
{@'- if or .sys.isVoid .sys.isDebianBased -'@}
{@' includeTemplate .tmpl.msg.managedByChezmoi . | comment "# " '@}
---

{@'- if .sys.isDebianBased '@}

- name: Set default browser (via update-alternatives)
  become: true
  community.general.alternatives:
    name: "{{ item }}"
    link: "/usr/bin/{{ item }}"
    path: "/usr/bin/{{ vbd_www_browsers_default_browser }}"
    state: selected
  loop:
    - gnome-www-browser
    - x-www-browser
  tags: [packages, desktop, www_browsers, set_default_browser]

{@'- end '@}

- name: Set default browser (via xdg-mime)
  community.general.xdg_mime:
    handler: "{{ vbd_www_browsers_default_browser }}.desktop"
    mime_types:
      - x-scheme-handler/http
      - x-scheme-handler/https
      - text/html
      - text/xml
      - application/xml
  tags: [packages, desktop, www_browsers, set_default_browser]

...
{@'- end '@}
