# chezmoi:template: left-delimiter={@ right-delimiter=@}
{@- if .sys.isVoid -@}
{@- includeTemplate .tmpl.msg.managedByChezmoi . | comment "# " @}
#

{@ $chromiumFlags := includeTemplate .tmpl.chromiumFlags (dict "app" "chromium" "userid" .chezmoi.uid "useWayland" .sys.useWayland) | fromYaml | join "\n" -@}

{@ $chromiumFlags -@}

{@- end @}
