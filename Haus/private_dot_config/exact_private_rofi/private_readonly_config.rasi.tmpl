/*
{{ includeTemplate .tmpl.msg.managedByChezmoi . }}
*/
configuration {
	font: {{ .rofi.font | quote }};
	cache-dir: {{ joinPath .chezmoi.homeDir .dir.localCache "rofi" | quote }};
}
@theme {{ .rofi.theme | quote }}
window {
	border-radius: 10px;
}
